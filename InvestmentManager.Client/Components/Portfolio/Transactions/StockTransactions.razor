@page "/stocktransactions"
@inject HttpClient httpClient

@if (models != null)
{
    foreach (var model in models)
    {
        string profit = model.CurrencyId == 1 ? model.ProfitCurrent.ToString("C", new CultureInfo("en-US")) : model.ProfitCurrent.ToString("C");
        Choices.Color lotColor = model.FreeLot == 0 ? Choices.Color.success : Choices.Color.light;
        Choices.Color profitColor = model.ProfitCurrent > 0 ? Choices.Color.success : Choices.Color.danger;

        <div class="container ir ibl iblbscd">
            <RowTitle Text="@model.CompanyName" Color="Choices.Color.warning"/>
            @if (_companyId == model.CompanyId)
            {
                <StockTransactionDetail CloseDetail="@CloseDetail" CompanyId="@model.CompanyId" />
            }
            else
            {
                <DetailIcon DetailAction="GetDetail" Value="@model.CompanyId.ToString()" />
                <RowInfo Title="Последняя операция:" Value="@model.DateLastTransaction.ToShortDateString()" />
                <RowInfo Title="Свободных лотов:" Value="@model.FreeLot.ToString()" Color="@lotColor" />
                <RowInfo Title="Текущий профит:" Value="@profit" Color="@profitColor" />
            }


            @if (model.ProfitCurrent < 0 && model.FreeLot > 0)
            {
                string price = model.CurrencyId == 1
                    ? ((decimal)Math.Abs(model.ProfitCurrent) / model.FreeLot).ToString("C", new CultureInfo("en-US"))
                    : ((decimal)Math.Abs(model.ProfitCurrent) / model.FreeLot).ToString("C");
                <RowInfo Title="Взвешенная цена:" Value="@price" Color="Choices.Color.info"/>
            }
        </div>
    }
}

@code {
    long _companyId = 0;
    IEnumerable<StockTransactionModel> models;
    protected override async Task OnInitializedAsync() =>
        models = await httpClient.GetFromJsonAsync<IEnumerable<StockTransactionModel>>("transaction/getstocktransactions");
    void GetDetail(string companyId) => _companyId = long.Parse(companyId);
    void CloseDetail() => _companyId = 0;
}
