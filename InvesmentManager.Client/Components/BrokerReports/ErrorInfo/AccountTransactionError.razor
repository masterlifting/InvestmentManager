<AlertInv AlertType="@alertType" />
<BaseError Title="@Title" ErrorValue="@ErrorValue" IsDone="@isDone"></BaseError>
@code {
    AlertType alertType;

    [Parameter] public string ErrorValue { get; set; }
    [Parameter] public string Title { get; set; }

    bool isDone = false;
}
